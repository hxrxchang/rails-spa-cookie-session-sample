import { WithCredentialInterceptor } from './with-credential-interceptor';

describe('WithCredentialInterceptor', () => {
  it('should create an instance', () => {
    expect(new WithCredentialInterceptor()).toBeTruthy();
  });
});
